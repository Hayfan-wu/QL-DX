# QL-DX - 中国电信话费自动化

基于 Playwright 的中国电信网上营业厅自动化脚本，**纯宿主机部署**，支持 **QQ 机器人交互控制** + `crontab` 定时运行。

## 功能

| 功能 | 说明 |
|------|------|
| 账号密码登录 | Cookie 持久化，一次登录持续有效 |
| 每日签到 | 自动签到翻牌领金豆/话费 |
| 口令兑换 | 输入省份口令抽话费(0.66~100元) |
| 活动扫描 | 遍历签到+口令兑换页面自动参与 |
| 限时秒杀 | 指定时间精准抢购 |

---

## 活动入口与产物一览

### 网页自动化活动（2个核心，脚本实际执行）

| 编号 | 活动名称 | 产物 | URL |
|------|----------|------|-----|
| ① | 签到翻牌抽奖 | 话费(0.1~100元)、金豆(20~1500)、流量包、生肖卡 | `wapact.189.cn:9001/mas-pub-ui/spm/Spring2024` |
| ② | 口令兑换 | 话费(0.66~100元) | `wapact.189.cn:9001/flcj/index.html` |

### APP 专属活动（3个，网页无法自动化，仅供参考）

| 编号 | 活动名称 | 产物 | 入口 |
|------|----------|------|------|
| ③ | 每日签到领金豆 | 金豆(20~35个/天)、连续签到额外抽奖 | APP首页 → 签到 |
| ④ | 金豆秒杀0.5元话费 | 0.5元话费 (100金豆兑换，每日10:00限量) | APP金豆商城 → 兑换区 |
| ⑤ | 金豆秒杀1元话费 | 1元话费 (200金豆兑换，每日14:00限量) | APP金豆商城 → 兑换区 |

---

## 部署步骤

### 一键部署

```bash
cd /opt && rm -rf QL-DX
git clone https://github.com/Hayfan-wu/QL-DX.git
cd /opt/QL-DX
pip install -r requirements.txt --break-system-packages
playwright install chromium
```

### 设置定时任务（crontab）

```bash
# 编辑 crontab
crontab -e

# 添加以下行（每天 8:00、12:00、18:00 执行）
0 8,12,18 * * * cd /opt/QL-DX && python3 main.py >> /opt/QL-DX/cron.log 2>&1
```

### 配置 QQ 机器人

QL-Bot 启动时自动扫描 `/opt/QL-DX/bot_plugins/` 并加载插件，无需额外配置。

在 QQ 群发送 `电信菜单` 确认插件已加载，然后发送 `电信登录` 按引导输入手机号和密码，配置自动保存到 `/opt/QL-DX/.env`。

---

## 环境变量

| 变量 | 说明 | 必填 |
|------|------|------|
| `DX_ACCOUNT` | 电信账号（格式: 手机号#密码） | ✅ |
| `DX_ENABLE_SIGNIN` | 启用签到 | 否 |
| `DX_ENABLE_ACTIVITY` | 启用活动扫描 | 否 |
| `DX_ENABLE_FLASH_SALE` | 启用秒杀 | 否 |
| `DX_FLASH_SALE_TIME` | 秒杀时间 HH:MM:SS | 否 |
| `DX_HEADLESS` | 无头模式 true/false | 否 |

---

## QQ 机器人命令

| 命令 | 功能 |
|------|------|
| `电信菜单` | 显示活动清单（含产物）和全部命令功能 |
| `电信登录` | 多轮引导：输入手机号 → 输入密码 → 保存到本地配置 |
| `电信状态` | 查看当前账号、密码状态、各功能开关、秒杀时间 |
| `电信签到` | 手动触发签到翻牌任务 |
| `电信执行` | 执行全部自动化任务 |
| `电信配置` | 查看所有环境变量当前值（账号脱敏显示） |
| `电信开启 签到` | 开启签到功能（可选: 签到/活动/秒杀） |
| `电信关闭 秒杀` | 关闭秒杀功能 |
| `签到` | 快捷命令，等于 `电信签到` |
| `话费` / `金豆` | 快捷命令，等于 `电信状态` |

### 交互流程示例

```
用户: 电信登录
Bot:  🔑 请输入你的中国电信手机号（11位数字）：

用户: 13800138000
Bot:  📱 手机号已记录，请输入登录密码：

用户: mypassword
Bot:  ✅ 账号密码已保存到本地配置！
      📱 手机号: 138****8000
      🔐 密码: **********
      
      配置已生效，可发送 电信执行 开始任务
      或发送 电信状态 查看配置状态

用户: 电信执行
Bot:  🚀 全部任务任务已提交执行
      进程 PID: 12345
      请稍后查看脚本日志获取结果
      日志文件: /opt/QL-DX/dx_telecom.log
```

## 项目结构

```
QL-DX/
├── main.py              # 脚本入口
├── telecom_api.py       # 核心自动化逻辑（登录+签到+口令兑换+秒杀）
├── config.py            # 配置管理（5个活动URL+产物）
├── .env                 # 环境变量（DX_ACCOUNT=手机号#密码）
├── .env.example         # 环境变量模板
├── requirements.txt     # Python 依赖
├── README.md            # 本文件
└── bot_plugins/         # QQ机器人插件
    ├── __init__.py
    └── dx_plugin.py     # 交互逻辑（全中文命令，配置本地保存）
```

## 注意事项

- 首次运行建议 `DX_HEADLESS=false`，确认流程正常后再开启无头模式
- 如遇验证码，无头模式下会失败，请先手动登录一次保存 Cookie
- 活动页面 URL 可能变动，可在 `config.py` 中更新
- 秒杀功能仅在脚本运行时间接近目标时间时自动触发，crontab 建议设在秒杀前几分钟
- `DX_ACCOUNT` 格式为 `手机号#密码`，用 `#` 分隔