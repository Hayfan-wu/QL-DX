# QL-DX - 中国电信话费自动化

基于 Playwright 的中国电信网上营业厅自动化脚本，支持**青龙面板定时运行** + **QQ 机器人交互控制** + **产物自动记录查询**。

## 功能

| 功能 | 说明 |
|------|------|
| 账号密码登录 | Cookie 持久化，一次登录持续有效 |
| 每日签到 | 自动签到翻牌领金豆/话费 |
| 口令兑换 | 输入省份口令抽话费(0.66~100元) |
| 活动扫描 | 遍历签到+口令兑换页面自动参与 |
| 限时秒杀 | 指定时间精准抢购 |
| 产物记录 | 每次执行自动记录产物，可随时查询 |

---

## 活动入口与产物一览

### 网页自动化活动（2个核心，脚本实际执行）

| 编号 | 活动名称 | 产物 | URL |
|------|----------|------|-----|
| ① | 签到翻牌抽奖 | 话费(0.1~100元)、金豆(20~1500)、流量包、生肖卡 | `wapact.189.cn:9001/mas-pub-ui/spm/Spring2024` |
| ② | 口令兑换 | 话费(0.66~100元) | `wapact.189.cn:9001/flcj/index.html` |

### APP 专属活动（3个，网页无法自动化，仅供参考）

| 编号 | 活动名称 | 产物 | 入口 |
|------|----------|------|------|
| ③ | 每日签到领金豆 | 金豆(20~35个/天)、连续签到额外抽奖 | APP首页 → 签到 |
| ④ | 金豆秒杀0.5元话费 | 0.5元话费 (100金豆兑换，每日10:00限量) | APP金豆商城 → 兑换区 |
| ⑤ | 金豆秒杀1元话费 | 1元话费 (200金豆兑换，每日14:00限量) | APP金豆商城 → 兑换区 |

---

## 快速开始

### 首次部署

```bash
cd /opt && git clone https://github.com/Hayfan-wu/QL-DX.git
cd QL-DX
pip install -r requirements.txt --break-system-packages
playwright install chromium
```

### 更新

```bash
cd /opt/QL-DX && git pull
```

### 青龙面板端

```bash
# 首次
cd /ql/data/scripts && git clone https://github.com/Hayfan-wu/QL-DX.git
cd QL-DX
pip install -r requirements.txt --break-system-packages
playwright install chromium

# 更新
cd /ql/data/scripts/QL-DX && git pull
```

### 配置环境变量

在青龙面板中创建以下环境变量，或通过 QQ 机器人 `电信登录` 设置（登录完成后自动提交青龙）：

| 变量 | 说明 | 必填 |
|------|------|------|
| `DX_ACCOUNT` | 电信账号（格式: 手机号#密码） | ✅ |
| `DX_ENABLE_SIGNIN` | 启用签到 | 否 |
| `DX_ENABLE_ACTIVITY` | 启用活动扫描 | 否 |
| `DX_ENABLE_FLASH_SALE` | 启用秒杀 | 否 |
| `DX_FLASH_SALE_TIME` | 秒杀时间 HH:MM:SS | 否 |
| `DX_HEADLESS` | 无头模式 true/false | 否 |

### 创建定时任务（重要）

青龙面板拉取仓库后会自动生成多个任务，**只需保留一个**，其余全部删除：

| 操作 | 文件名 | 说明 |
|:---|:---|:---|
| ✅ 保留 | `main.py` | 唯一入口脚本 |
| ❌ 删除 | `config.py` | 配置文件，不是任务 |
| ❌ 删除 | `__init__.py` | 包文件，不是任务 |
| ❌ 删除 | `telecom_api.py` | 模块文件，不是任务 |
| ❌ 删除 | `bot_plugins/dx_plugin.py` | 插件文件，不是任务 |

定时任务配置：

```
任务名称: DX-Telecom
命令: task QL-DX/main.py
定时规则: 0 8,12,18 * * *
```

---

## QQ 机器人交互

本仓库自带 `bot_plugins/` 插件，配合 [QL-Bot](https://github.com/Hayfan-wu/QL-Bot) 框架使用，零侵入接入。

### 部署方式

```bash
# 首次
cd /opt && git clone https://github.com/Hayfan-wu/QL-DX.git

# 更新
cd /opt/QL-DX && git pull
```

QL-Bot 启动时自动扫描 `/opt/QL-DX/bot_plugins/` 并加载插件。

### 机器人命令（全部中文）

| 命令 | 功能 |
|------|------|
| `电信菜单` | 显示活动清单（含产物）和全部命令功能 |
| `电信登录` | 多轮引导：输入手机号 → 输入密码 → 自动保存并提交青龙面板 |
| `电信状态` | 查看当前账号、密码状态、各功能开关、秒杀时间 |
| `电信查询` | 查询所有历史任务获得的产物（累计+详情） |
| `电信签到` | 手动触发签到翻牌任务（subprocess 异步，不阻塞） |
| `电信执行` | 执行全部自动化任务（登录→签到→活动扫描→秒杀） |
| `电信配置` | 查看所有环境变量当前值（账号脱敏显示） |
| `电信开启 签到` | 开启签到功能（可选: 签到/活动/秒杀） |
| `电信关闭 秒杀` | 关闭秒杀功能（可选: 签到/活动/秒杀） |
| `签到` | 快捷命令，等于 `电信签到` |
| `话费` / `金豆` | 快捷命令，等于 `电信状态` |

### 交互流程示例

```
用户: 电信登录
Bot:  🔑 请输入你的中国电信手机号（11位数字）：

用户: 13800138000
Bot:  📱 手机号已记录，请输入登录密码：

用户: mypassword
Bot:  ✅ 账号密码已保存！
      📱 手机号: 138****8000
      🔐 密码: **********
      
      📤 已自动提交到青龙面板
      ✅ 成功: 6 个
      请在青龙面板中创建定时任务...

用户: 电信查询
Bot:  📋 电信任务产物查询
      ━━━━━━━━━━━━━━━━━━━━
      📅 最近执行: 2026-07-17 18:00:05
      🔄 累计执行: 3 次
      ━━━━━━━━━━━━━━━━━━━━
      📦 累计获得产物:
        签到: 3 次
          └ 2026-07-17 18:00:05: 签到成功，金豆约 35
          └ 2026-07-17 12:00:03: 签到成功，金豆约 28
          └ 2026-07-17 08:00:02: 签到成功，金豆约 20
        活动: 3 次
          └ 2026-07-17 18:00:05: 参与 2 个
      ━━━━━━━━━━━━━━━━━━━━
      📝 最近一次详情:
        签到: 签到成功，金豆约 35
        活动: 参与 2 个
```

## 项目结构

```
QL-DX/
├── main.py              # 唯一入口（青龙只需这一个任务）
├── telecom_api.py       # 核心自动化逻辑（登录+签到+口令兑换+秒杀+产物记录）
├── config.py            # 配置管理（5个活动URL+产物）
├── .env                 # 环境变量（DX_ACCOUNT=手机号#密码，git不追踪）
├── .env.example         # 环境变量模板
├── result.json          # 产物记录（自动生成，每次执行后写入）
├── requirements.txt     # Python 依赖
├── README.md            # 本文件
└── bot_plugins/         # QQ机器人插件
    ├── __init__.py
    └── dx_plugin.py     # 交互逻辑（全中文命令，登录后自动提交青龙）
```

## 注意事项

- 首次运行建议关闭无头模式（`DX_HEADLESS=false`），确认流程正常后再开启
- 如遇验证码，无头模式下会失败，请先手动登录一次保存 Cookie
- `DX_ACCOUNT` 格式为 `手机号#密码`，用 `#` 分隔
- 青龙面板拉取后**务必删除多余任务**（config.py/__init__.py/telecom_api.py 等），只保留 main.py
- 产物记录保存在 `result.json`，通过 `电信查询` 命令查看